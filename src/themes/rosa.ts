import ThemeType from "./theme-Type";

const rosa: ThemeType = {
  primary: {
    default: "DeepPink",
    contrast: "white",
  },
  secondary: {
    default: "gray",
    contrast: "white",
  },
};

export default rosa;
